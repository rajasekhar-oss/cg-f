export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080', // change if needed
  wsUrl: 'ws://localhost:8080/ws' // STOMP websocket endpoint (adjust)
};

// Placeholder service for compatibility
export class EnviornmentsService {
  constructor() { }
}
