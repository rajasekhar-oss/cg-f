import { Routes } from '@angular/router';
import { UserTodoComponent } from './user-todo.component';

export const userTodoRoutes: Routes = [
  { path: 'user/todos', component: UserTodoComponent }
];
